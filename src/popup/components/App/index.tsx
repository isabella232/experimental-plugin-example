import * as React from 'react'
import styled, { keyframes } from 'styled-components'
// just ensure the build
const icon = require('../../../assets/icon.png')
const icon2 = require('../../../assets/icon2.png')

const App = styled.div`
  text-align: center;
`

const Comp = () => (
  <App>
    
  </App>)

export default Comp
